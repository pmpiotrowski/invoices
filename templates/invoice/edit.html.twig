{% extends 'base.html.twig' %}

{% block title %}Edycja faktury{% endblock %}

{% block body %}
    <h1 class="mt-4">Edycja faktury</h1>

    <a class="btn btn-primary" href="{{ path('invoice_index') }}">Powrót</a>

    {{ include('invoice/_form.html.twig', {'button_label': 'Zapisz'}) }}

    {{ include('invoice/_delete_form.html.twig') }}
{% endblock %}
